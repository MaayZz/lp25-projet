#ifndef UI_H
#define UI_H

/**
 * Lance l'interface ncurses en mode local uniquement.
 * C'est la "boucle principale" côté interface (affichage + clavier).
 */
void ui_run_local(void);

#endif /* UI_H */
